/**
 * @summary
 * External (public) API routes configuration
 *
 * @module routes/v1/externalRoutes
 *
 * @description
 * Configures public API endpoints that do not require authentication.
 */

import { Router } from 'express';

const router = Router();

export default router;
